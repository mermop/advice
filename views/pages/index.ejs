<!doctype html>
<html>
<head>
  <title>Hello world</title>
</head>
<body>
  <h1>Hello world</h1>
  <ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> - <%= r.name %></li>
    <% }); %>
  </ul>
</body>
</html>
